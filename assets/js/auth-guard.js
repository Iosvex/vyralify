import {auth,db} from './firebase-config.js';import {onAuthStateChanged} from 'https://www.gstatic.com/firebasejs/10.12.2/firebase-auth.js';import {doc,getDoc} from 'https://www.gstatic.com/firebasejs/10.12.2/firebase-firestore.js';
export let currentProfile=null;export const profileReady=new Promise(resolve=>onAuthStateChanged(auth,async u=>{if(!u){location.replace('/login.html');return}const s=await getDoc(doc(db,'users',u.uid));currentProfile=s.data()||{};if(document.body.dataset.admin==='true'&&currentProfile.role!=='admin'){location.replace('/dashboard.html');return}resolve({user:u,profile:currentProfile})}));
