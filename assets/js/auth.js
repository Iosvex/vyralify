import {auth,db} from './firebase-config.js';import {createUserWithEmailAndPassword,signInWithEmailAndPassword,GoogleAuthProvider,signInWithPopup,onAuthStateChanged,signOut} from 'https://www.gstatic.com/firebasejs/10.12.2/firebase-auth.js';import {doc,setDoc,serverTimestamp} from 'https://www.gstatic.com/firebasejs/10.12.2/firebase-firestore.js';
const $=s=>document.querySelector(s);const message=t=>{const e=$('.form-error');if(e)e.textContent=t};
const signup=$('#signup-form');if(signup)signup.addEventListener('submit',async e=>{e.preventDefault();message('');const f=new FormData(signup);try{const c=await createUserWithEmailAndPassword(auth,f.get('email'),f.get('password'));await setDoc(doc(db,'users',c.user.uid),{email:c.user.email,displayName:f.get('name')||'',role:'member',tier:'free',country:f.get('country')||'',currency:(f.get('country')||'').toUpperCase()==='IN'?'INR':'USD',billingProvider:(f.get('country')||'').toUpperCase()==='IN'?'razorpay':'stripe',onboardingTrack:f.get('track')||null,createdAt:serverTimestamp(),updatedAt:serverTimestamp()},{merge:true});location.href='/dashboard.html'}catch(err){message(err.code==='auth/email-already-in-use'?'This email already has an account.':err.message)}});
const login=$('#login-form');if(login)login.addEventListener('submit',async e=>{e.preventDefault();message('');const f=new FormData(login);try{await signInWithEmailAndPassword(auth,f.get('email'),f.get('password'));location.href='/dashboard.html'}catch{message('Could not sign in. Check your email and password.')}});
$('#google-login')?.addEventListener('click',async()=>{try{await signInWithPopup(auth,new GoogleAuthProvider());location.href='/dashboard.html'}catch{message('Google sign-in could not be completed.')}});window.vyralifyLogout=()=>signOut(auth).then(()=>location.href='/index.html');
